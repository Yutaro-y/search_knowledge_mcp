# search-knowledge-mcp 対応履歴

## 1. 背景
本プロジェクトは、インターネット上の最新技術情報を収集し、MCPサーバとしてAIエージェントから使えるようにする目的で着手した。

収集対象として重視したのは、以下のような情報である。
- リファレンス
- NW機器のコマンドリファレンス
- 設定例
- OS / ライブラリ / パッケージ / SDK / メーカー製品の細かい仕様
- CVE や既知バグ、その回避策・修正版情報
- ファームウェアやOSなどのアップデート、リリースノート、仕様変更情報

また、検索基盤には OpenAI を活用し、最新情報を取得しやすくすること、URLエラーなどにより情報取得に失敗しにくい構成にすることが要求された。

## 2. 初期要件整理
当初、ユーザからは以下の条件が提示された。
- プロジェクトルートは `/home/user/projects/mcpServers/search_knowledge_mcp`
- 既にルート直下に仕様書案3件が配置されている
- それらを読んだうえで実装する
- uv 前提でプロジェクトを構築する
- 将来的に GitHub / PyPI に登録したい
- 今回は登録までは行わないが、README、仕様書、Qiita記事草案を作成する

この段階で、実装前にまず仕様書案を読んで要件を統合する方針を採った。

## 3. 仕様書案の確認と設計方針の統合
仕様書案3件を確認した結果、以下の方針を採用した。
- MCPサーバは Python + FastMCP + stdio で実装する
- 検索基盤は OpenAI Responses API + Web Search を利用する
- 公開ツールは「統合ツール1本」と「用途別ラッパ」を両方用意する
- 内部実装は責務分離し、今後の拡張や保守をしやすくする

ここで、パッケージ名は最終的に以下で確定した。
- 配布名: `search-knowledge-mcp`
- import名: `search_knowledge_mcp`

## 4. 初期実装
新規プロジェクトとして、以下の構成を作成した。
- `pyproject.toml`
- `src/search_knowledge_mcp/`
- `tests/`
- `docs/`
- `.env.example`

そのうえで、以下の主要モジュールを実装した。
- `config.py`: 環境変数と設定管理
- `logging_utils.py`: ログ設定
- `schemas.py`: 入出力スキーマ
- `clients/openai_client.py`: OpenAI Responses API + Web Search 呼び出し
- `search/query_builder.py`: 検索クエリ生成
- `search/classifier.py`: ソース種別やカテゴリの推定
- `search/parser.py`: OpenAI応答の構造化
- `server.py`: MCPツール公開

公開ツールとしては以下を用意した。
- `search_network_knowledge`
- `search_network_docs`
- `search_os_and_software_specs`
- `search_vulnerabilities_and_bugs`
- `search_release_notes_and_updates`
- `search_freeform_tech_info`

## 5. 品質確認と初期不具合修正
実装後、`uv sync`、`ruff check .`、`pytest` による確認を進めた。

このフェーズでは主に以下を修正した。
- `from __future__ import annotations` と Ruff の整形ルールの衝突解消
- import順や行長超過の修正
- Pydantic / typing まわりの軽微な整形
- APIキー未設定時でも import 時点で落ちないように変更

特に、MCPサーバとしては「起動時に即死する」よりも「ツール呼び出し時に設定エラーを返す」方が扱いやすいため、`OPENAI_API_KEY` 未設定時は構造化エラーを返す設計へ調整した。

## 6. 実検索確認と OpenAI 接続不具合の解消
その後、`.env` に OpenAI APIキーを投入したうえで、実検索確認を実施した。

確認対象の例:
- Cisco IOS XE コマンドリファレンス
- OpenSSH 関連CVE
- Ubuntu 24.04 リリースノート

初回実行時には `httpx.UnsupportedProtocol` が発生した。
調査の結果、OpenAI SDK の `base_url` が空文字となって `/responses` に対して不正なURLでアクセスしていたことが原因と判明した。

このため、以下を修正した。
- `OPENAI_BASE_URL` の空文字を `None` として扱う
- OpenAIクライアント生成時に `https://api.openai.com/v1` を安全なデフォルトとして明示適用する

修正後、OpenAI Responses API + Web Search の実検索は成功し、`200 OK` を確認した。

## 7. 検索品質の改善
実検索結果を確認したところ、以下の改善余地があった。
- URL重複がある
- source_type の分類が粗い
- 一部公式情報とコミュニティ情報の差が戻り値から見えにくい

このため、以下の改善を実施した。
- URL単位の重複排除
- `classifier.py` による source_type 改善
- 公式ドメインやセキュリティ系ドメインの判定強化

## 8. GitHub / PyPI 公開準備
次に、将来の公開に備えて、以下を整備した。
- `.gitignore`
- `LICENSE`
- `CHANGELOG.md`
- `CONTRIBUTING.md`
- `.github/workflows/ci.yml`
- `README.md`
- `docs/仕様書.md`
- `docs/qiita_article.md`

また、`pyproject.toml` のメタデータや `uv build` によるビルド確認も行い、wheel / sdist の生成を確認した。

## 9. 今回の追加改善: ソース信頼判断を支援する戻り値
追加要件として、クライアント側AIが「どのURLからどのような情報を得たのか」「その情報をどの程度信頼するか」を判断しやすくする必要が出た。

このため、検索結果 `results[]` に以下の情報を追加した。
- `content_kind`: 内容種別
- `content_observation`: そのURLから得た内容の短い説明
- `trust_level_hint`: high / medium / low
- `why_this_trust_level`: 信頼度ヒントの理由
- `trust_signals`: 公式性、CVE有無、バージョン手掛かり、ドメイン等

これにより、クライアントAIは以下のような判断がしやすくなった。
- 公式リファレンスやPSIRTなら強く採用する
- コミュニティ記事は具体例として参考にするが、適用前に公式資料と照合する
- ブログ記事は補助情報として扱う

## 10. 今回の追加改善: 本文取得レイヤと文字化け対策
追加要件として、URL先コンテンツをより多くクライアントAIへ渡せるよう、検索結果URLの本文取得レイヤを導入した。

この対応では以下を実施した。
- URL本文取得をデフォルトON、必要時のみOFF可能な設計へ変更
- `include_page_content` 引数をツールに追加
- HTTP取得したHTMLから本文テキストを抽出
- 本文から手順、コマンド候補、注意点、抜粋を構造化して返すようにした
- `page_content_summary` や `recommended_usage` を追加し、クライアントAIが結果の使い方を判断しやすくした

さらに、その後のクライアント組み込み検証において、Google Chrome + Streamlit 構成で一部日本語ページの文字化けが確認された。
AIの回答自体は概ね正しく生成されていたことから、主因は本文取得時の文字コード判定にあると判断した。

そこで以下の対策を実施した。
- `httpx.Response.text` 任せのデコードをやめ、`response.content` から明示的にデコードするよう変更
- `Content-Type` ヘッダの charset を優先判定
- HTML先頭付近の `meta charset` を抽出して判定に利用
- 日本語系エンコーディング (`cp932`, `shift_jis`, `euc_jp`, `iso2022_jp`) をフォールバック候補へ追加
- 文字列は Python 内部で Unicode として扱い、JSON返却時に UTF-8 前提で安定しやすい形へ寄せた

この対応により、日本語マニュアルやベンダーサイトの本文取得で文字化けしにくい実装へ改善した。

## 11. 今回の追加改善: 余分な改行コードの正規化
クライアントAIへの組み込み後の追加検証において、ツール実行結果の戻り値に、可読性を下げる量の連続改行コードが含まれるケースが確認された。

特に影響が見られたのは以下の項目である。
- `summary`
- `raw_snippet`
- `content_observation`
- `source_excerpt`
- 本文抽出由来の `extracted_facts` / `possible_procedures` など

原因としては、HTML本文抽出時に改行が多めに残ること、および検索結果本文や本文抽出結果をそのままレスポンスへ格納していたことが挙げられる。

このため、以下の対策を実施した。
- 返却用テキストの共通正規化関数 `normalize_text_for_output` を追加
- 改行コードを `\n` に統一
- 余分な連続改行を圧縮
- 行頭・行末付近の不要空白を除去
- `SearchResultItem` の model validator で、主要文字列フィールドと配列要素を返却前に一括正規化
- HTML本文抽出時にも改行圧縮を強化

この対応により、クライアントAIや Streamlit / Chrome 側で結果を表示した際に、情報量は維持しつつ、空行が過剰に連続しない見やすい戻り値へ改善した。

今回の対応の流れは以下のとおりである。
1. クライアントAIへ再度組み込み、実画面上で戻り値の可読性を確認した
2. 連続改行が多いフィールドを洗い出し、本文抽出由来か検索結果由来かを切り分けた
3. HTML本文抽出側だけでなく、返却直前にも全体正規化を入れる方針を採用した
4. 文字列フィールドと配列要素の双方に対して共通正規化を適用した
5. テストを追加し、`ruff` / `pytest` を再実行して回帰がないことを確認した

この対応により、クライアントAIや Streamlit / Chrome 側で結果を表示した際に、情報量は維持しつつ、空行が過剰に連続しない見やすい戻り値へ改善した。

## 12. 現在の到達点
現時点で、本プロジェクトは以下の状態にある。
- MCPサーバとして起動可能
- OpenAI Web Search による実検索成功確認済み
- URL本文取得と本文由来の構造化抽出が可能
- 日本語ページ向けの文字コード対策を実装済み
- lint / test / build 通過済み
- GitHub / PyPI 公開準備済み
- 仕様書、README、Qiita記事草案、対応履歴を整備済み

## 13. 今後の改善候補
今後の拡張候補としては、以下がある。
- URLのHTTP HEAD / GETによる生存確認
- ベンダー別のドメイン辞書拡張
- CVE / JVN / NVD 向けの抽出精度向上
- キャッシュやレートリミット機構
- HTML本文抽出のノイズ除去強化
- README / project.urls / authors の実公開値への差し替え

以上が、当初要件から現在の完成形に至るまでの対応経緯である。
